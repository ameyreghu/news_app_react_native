import { ActivityIndicator, View } from "react-native";
import { Axios } from "axios";

export const DailyNewsScreen=()=>{
    return (
        <View></View>
    );
}


const Loading=()=>{
    return (
        <View>
            <ActivityIndicator/>
        </View>
    );
}